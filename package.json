{
  "name": "ai-technical-interviewer-monorepo",
  "version": "1.0.0",
  "description": "Unified repository for AI Technical Interviewer Platform",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:ai": "cd ai-analysis && streamlit run app.py",
    "setup:ai": "cd ai-analysis && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
